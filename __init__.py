"""What DIDA and BABA do the same way on the server: supervised background
tasks, the liveness file, plugins found by entry point, the login limits and
the session primitives. Each product keeps its own policy and passes it in."""
